# BUILD-LOG — movers-makers-shakers/web

Last updated: 2026-06-03 (**F032 (T091+T092)** — public Member page `/m/[handle]` + member→member follow, **merged to main**. `member.follow`/`member.unfollow` handlers + server actions; `resolveMemberPage` + `<MemberPublicPage>` + `<FollowMemberButton>`; additive migration `029` for the anon-readable standing-presence grant + `member_public_group_memberships` projection. F032 Playwright eval 6/6 GREEN. member_follows lights up end-to-end here — F035's deferred group-follow remains F042. **F041 (T093+T094)** — QR card generator, **merged to main**: print-quality PNG (1200px @ 300 DPI) for any Item kind, resolving to the canonical URL; owner-only "Get a QR card" affordance on product/service/gathering pages; `item.qr_card.request` action handler; F041 eval 4/4 GREEN.) (F040 + F034 both merged to main. **F040 (T081–T083)** — service composer: `item.create` service arm writes `service_area_geography` + `<ServiceComposer>` + `createServiceAction` + `<AddServiceButton>` + `resolveService`/`<ServicePublicPage>` + `/s/` dispatch + `/m/[handle]/s/[slug]` route; F040 Playwright eval 6/6 GREEN. **F034 (T084+T085, renumbered from T081/T082 to resolve the F040 collision)** — gathering composer + public gathering Item page: `<GatheringComposer>` (kind picker → details → schedule → review) + `createGatheringAction` + `<AddGatheringButton>` in the `/you/sell` COMPOSERS array; `resolveGathering` + `<GatheringPublicPage>` (next-occurrence + recurrence + Share-link) + `/e/` dispatch + `/m/[handle]/e/[slug]` route; F034 Playwright eval GREEN. Both `/s/` and `/e/` dispatch cases coexist in the `/p` catch-all.)

Development agent's build progress tracker. Use JOURNAL.md for product/strategy notes.

## Current Release

**Target:** b1 MVP — Producer Marketplace
**Start Date:** 2026-04-09
**Bundle definition:** [planning/now/bundle-1.md](../planning/now/bundle-1.md)

## Progress

| Ticket | Title | Status |
|--------|-------|--------|
| T001 | Project Initialization | ✅ Complete |
| T002 | Database Schema + Supabase Setup | ✅ Complete |
| T003 | Authentication | ✅ Complete |
| T004 | Map View with Colored Pins | ✅ Complete |
| T005 | Pin Clustering | ✅ Complete |
| T006 | Map Search | ✅ Complete |
| T007 | Business Detail Card | ✅ Complete |
| T008 | Business Registration | ✅ Complete |
| T009 | Shareable Listing | ✅ Complete |
| T010 | Support Button | ✅ Complete |
| T011 | Report a Concern | ✅ Complete |
| T012 | Market Schema + Seed Data | ✅ Complete |
| T013 | Bottom Navigation Shell | ✅ Complete |
| T014 | Home Feed (Etsy-style — superseded by T024) | ✅ Complete |
| T015 | Explore — Search + List/Map | ✅ Complete |
| T016 | Market Selection Modal | ✅ Complete |
| T017 | Vendor Profile Update | ✅ Complete |
| T018 | Follow Vendor + Notifications | ✅ Complete |
| T019 | Geocoding + Pin Confirmation | ⬜ Open (b1) |
| T020 | Community Pin Flagging | ⏸️ Deferred (b2) |
| T021 | Tide Accent + CTA Patterns | ✅ Complete |
| T022 | Foundational Schema (events, bulletins, vendor analytics, follow soft-delete) | ✅ Complete |
| T023 | `/you` Restructure (Saved/Following/Settings, vendor mode entry) | ✅ Complete |
| T024 | Events-driven Home Feed | ✅ Complete |
| T025 | Vendor Bulletin Compose + Delivery (T1) | ✅ Complete |
| T026 | Vendor Founder Dashboard (T1) | ✅ Complete |
| T027 | Event Surfacing on Profiles | ⏸️ Deferred (b2) |
| T028–T040 | STALE — pre-rebuild migration tickets | ⛔ STALE-banned 2026-05-09 |
| T041 | Phase 0 — Postgres extensions + embedding tables | ✅ Build + eval complete (22/22 floor.spec.ts 2026-05-18) |
| T042 | Phase 0 — Members + member_events floor + system Member | ✅ Build + eval complete (22/22 floor.spec.ts 2026-05-18) |
| T043 | Phase 0 — Action layer scaffold + `member.create` handler | ✅ Build + eval complete; 2 handler fix-forwards landed during eval (SAVEPOINT retry-loop; inline savepoint name) |
| T044 | Phase 0 — Supabase Auth signup hook → `member.create` | ✅ Build + eval complete (22/22 floor.spec.ts 2026-05-18) |
| T045 | Phase 1 — Locations spine + 3 children + events (`007_locations.sql`) | ✅ Build complete; eval 7/8 green via T053 (locations:254 spec-regex precision deviation — pipeline-eval fix-forward) |
| T046 | Phase 1 — Locations RLS fix-forward (`008_locations_owner_read.sql`) | ✅ Build + eval complete via T053 |
| T047 | Phase 1 — Members augmentation: FK + privacy + handle history (`009_members_phase1.sql`) | ✅ Build complete + M2 PROCEED; eval complete via T053 |
| T048 | Phase 1 — Member interests + follows (`010_member_interests_follows.sql`) | ✅ Build complete + M2 REQUEST→resolved (follow-visibility privacy gate dropped per PM); eval complete via T053 |
| T051 | Phase 0 — Action layer CI enforcement (four rules) | ✅ Build + eval complete (conformance check returned `{ok: true, violations: []}` in the 22/22 run) |
| T049 | Phase 1 — Member location affinities (`011_member_location_affinities.sql`) | ✅ Build complete; eval complete via T053 |
| T050 | Phase 1 — Agent-assistance substrate (`012_member_agent_assistance.sql`) | ✅ Build complete + M2 PROCEED; eval 6/7 green via T053 (members-agent-assistance:204 empty-scopes CHECK — T054 fix-forward) |
| T052 | Phase 0 — Eval helpers (test-helpers folder + bootstrap script) | ✅ Build + eval complete; 4 fix-forwards landed during PM eval pass (ADR-15 seed-auth-user, SAVEPOINT, serial-collision, synthetic email) |
| T053 | Phase 1 — Eval helpers (4 introspection RPCs appended to `00_introspection.sql`) | ✅ Build + eval complete + M2 PROCEED; 9/10 listed tests flipped green, +1 stale T052 Vitest assertion fixed inline; 2 deviations logged |
| T054 | Phase 1 — Fix empty-scopes CHECK on `member_delegations` (`013_member_delegations_scopes_check_fix.sql`) | ✅ Build + eval complete + M2/M4 PROCEED; `cardinality(scopes) >= 1` replaces broken `array_length(scopes, 1) >= 1`; 80/80 Phase 1 green |
| T055 | Phase 1 — Groups schema (`014_groups.sql` + `groups.spec.ts`) | ✅ Build + eval complete + M2/M4 PROCEED; spine + 2 children + memberships + partitioned events + standing-tier view; SECURITY DEFINER helper `current_member_explicit_group_ids()` resolves cross-table RLS recursion (42P17); 25/25 groups tests + 105/105 phase-1 |
| T056 | Phase 1 — Items schema (`015_items.sql` + `items.spec.ts`) | ✅ Build + eval complete + M2/M4 PROCEED; spine + 4 kind children + 4 join tables + partitioned events; state enum reconciled to (draft/published/withdrawn/fulfilled/closed); closes T055's deferred FK on group_event_anchored.seeded_by_item_id; 27/27 items tests + 132/132 phase-1 |
| T057 | Phase 1 — discoverable_items view + refresh trigger (`016_discoverable_items.sql` + `discoverable-items.spec.ts`) | ✅ Build + eval complete + M2/M4 PROCEED; materialized view with SECURITY DEFINER refresh on `item.published` events; CONCURRENTLY refresh + unique index; anon-readable; group / soft-delete / response-count / nearest-location semantics verified; 10/10 disc-items + 142/142 phase-1; **closes Phase 1 substrate** |
| T058 | b1.x — `places` table + launch-locality seed (`017_places.sql` + `places.spec.ts`) | ✅ Build + eval complete + M2/M4 PROCEED; 19/19 unit + 14/14 Phase-1 evals green. Hierarchy + parent-scoped slug uniqueness + GIST polygon index + RLS public-read + place_events partitioned. Seeds 9 rows (California → MSA → Sacramento → 6 neighborhoods). |
| T059 | b1.x — Reverse-geocoder + `place_for_coords` PG function (`022_*` + `reverse-geocode.ts`) | ✅ Build + eval complete + M2 PROCEED; 12/12 unit + 7/7 Phase-1 evals green. SECURITY DEFINER ST_Covers with smallest-polygon-wins; LRU cache (1k @ 24h, 4-dp quantize); Mapbox fallback with slug-match; graceful nulls on Mapbox 5xx / missing token. |
| T060 | b1.x — Place-scoped URL routing (`/p/[...slug]` + `resolve-path.ts` + `place-breadcrumb.tsx`) | ✅ Build + eval complete + M2/M3 PROCEED; 10/10 unit + 7/7 Phase-1 evals green. Next.js catch-all + parent_id chain resolver + ARIA breadcrumb. **b1.1 constraint flagged in DEVIATIONS:** Next.js does not allow a static segment after a catch-all, so Groups-under-place URLs must dispatch inside this same page.tsx. |
| T061 | b1.x — Retire `member_location_affinities` (`021_*`) | ✅ Build + eval complete + M2/M4 PROCEED; 6/6 unit tests + Phase-1 floor green. Drops table + 3 SECURITY DEFINER functions; rewrites `member_events.event_kind` CHECK to drop 2 retired kinds; deletes `members-affinities.spec.ts`; patches `floor.spec.ts` + `members-interests-follows.spec.ts`; removes from action-layer PROTECTED_TABLES. |
| T062 | b1.x — `member_place_interests` + `place_interest_add/remove` handlers (`018_*`) | ✅ Build + eval complete + M2 PROCEED; 12/12 schema + 11/11 handler unit + 10/10 Phase-1 evals green. Owner-only RLS + partial UNIQUE for single active primary_home + atomic-swap promotion + SECONDARY_LIMIT=5 cap. |
| T063 | b1.x — `member_saved_searches` + 3 handlers (`019_*`) | ✅ Build + eval complete + M2 PROCEED (2 existence-leak findings auto-fixed); 11/11 schema + 18/18 handler unit + 9/9 Phase-1 evals green. At-least-one-filter CHECK + owner-only RLS + place/location/interest_tags filters; update/remove collapse not-owner → NotFoundError for privacy parity. |
| T064 | b1.x — `items.made_at_*` columns + event-kind extension (`020_*`) | ✅ Build + eval complete + M2/M4 PROCEED; 8/8 unit tests green. 4-value `made_at_verification_source` enum (includes `community_attested` per ADR-21 reshape) + FK to `places` + `items_made_at_only_on_products` CHECK. Includes doc-patch to `planning/rebuild-plan.md:148` (3-value → 4-value form). |
| T065 | Stryker mutation testing on `src/lib/` (substrate) | ✅ Tooling landed; `npm run mutate` runs end-to-end. Baseline: **30% total mutation score**, `market-dates.ts` 82.5% (only file with sibling unit tests), 4 others 0%. Runtime ~3 min. Local-only; no CI gate. Surfaces a real coverage gap — follow-up work needed to add unit tests for `slugify.ts`, `categories.ts`, `geocoding.ts`, `action-context.ts`. |
| T068 | Stryker test-runner scope fix (substrate) | ✅ Config narrowed — `tests/**/*.test.ts(x)` now participates; exclude trimmed to `migrations-*.test.ts` + 5 named pre-existing stale files (deferred to T069). New baseline: **58.05% total mutation score** (was 30%). Per-file: `resolve-path.ts` 72.22% (was 0%), `reverse-geocode.ts` 63.53% (was 0%), `geocoding.ts` 60.00% (was 0%), `slugify.ts` 30.43% (was 0%), `market-dates.ts` 82.50% (unchanged), `action-context.ts` 0%, `categories.ts` 0% (no tests exist — follow-up). Runtime 5m59s (was ~3min). |
| T070 | Groups lifecycle_state + draft-aware RLS + group.create / .update_draft / .activate handlers (F036 substrate) | ✅ Migration `023_groups_lifecycle_state.sql` (lifecycle_state enum + idx_groups_lifecycle + groups_select_active_or_own_draft RLS + group_events.event_kind extended with group.activated). Three handlers in `src/actions/group/{create,update-draft,activate}.ts` registered in `src/actions/index.ts`; `_lib/event-log.ts` extended for group_events table. 40/40 vitest tests GREEN. Action-layer conformance OK. M2 code-review: 2 critical fixes landed pre-commit — TOCTOU re-assertion of lifecycle_state='draft' in both update_draft UPDATEs (spine + group_businesses); random-hex suffix on draft slugs to prevent concurrent-create collisions on the slug UNIQUE constraint. Plus M2 disposition pass: dropped `group.member_removed` from CHECK (no producer); extracted `GROUP_KINDS` + `DRAFT_NAME_PLACEHOLDER` to `src/actions/group/constants.ts`. Commit `d8204c7`; merged to main as `39b11df`. |
| T071 | `<MultiStepComposer>` base + dev demo page (F036 surface, shared by F034/F038/F040) | ✅ Generic, kind-agnostic, presentational + control-flow composer at `src/components/composer/MultiStepComposer.tsx` per `product/ui/design-language.md § Component recipes → Multi-step composer`. Container per ADR-2: bottom-anchored drawer on mobile, modal on desktop. Step indicator `role=progressbar` with `aria-valuenow/valuemax`; navigation row Back / Skip (optional only) / Continue; final-step CTA reads step-supplied `finalLabel`; tap-outside-to-dismiss OFF; X-button + ESC fire `onAbandon`; focus restored to opener on unmount; inline field errors + submit-error region; spinner-on-submit disables Continue. 13/13 vitest GREEN. M2 self-review PROCEED after ESC + focus-restore fixes; M3 satisfied at the basic level (full focus-trap deferred). Dev demo at `/composer-demo`. Commit `d31f3ff`; merged to main as `3c64571`. |
| T071a | Gate `/(dev)` routes to NODE_ENV=development only (T071 retroactive M4 punch list) | ✅ One-file addition `src/app/(dev)/layout.tsx` returning `notFound()` on non-dev environments. Covers `/composer-demo` (T071) + all future `(dev)/*` routes. Commit `c7d9f3e`; merged to main as `8da39a4`. |
| T074 | F035 public Shop page at `/p/[…place]/g/[slug]` (read surface) | ✅ Build complete on branch `t074`. Group dispatch folded into the place catch-all `src/app/p/[...slug]/page.tsx` per T060 DEVIATION (Next.js forbids a static segment after a catch-all). New `src/lib/groups/resolve-shop.ts` (`splitGroupSlug`, `resolveShop`, `resolveShopItems`, `resolveLocalOwnerBadge`) + `src/components/group/{ShopPublicPage,FollowShopButton}.tsx`. RLS (T070 `groups_select_active_or_own_draft`) is the visibility gate — draft/dissolved/nonexistent → 404; founder's own draft → owner preview banner. Beats 1/3/5/6 shipped fully; Beat 2 (Claimed-local-owner badge) render-path present but data deferred to F037/S-jurisdictions; Beat 4 follow **persistence** deferred to F042 (group-follow substrate doesn't exist — `member_follows` is member→member only). 24 new vitest (resolve-shop 13 + ShopPublicPage/FollowShopButton 11); 98/98 src GREEN. tsc clean on T074 files; action-layer conformance OK. M2 PROCEED (no criticals; `splitGroupSlug` positional-`g` note + FK-hint runtime-verify). M3 PASS (basic) after decorative-avatar alt fix. DEVIATIONS + SPEC-PATCHES (group-follow schema gap) logged. **F035 Playwright eval run 2026-06-02: 8/8 GREEN** (all 6 beats incl. the 3 logged-in flows). Root-caused a GoTrue `500 "converting NULL to string is unsupported"` on every password login to NULL token columns on 216 pre-existing auth.users rows (older seed path); healed the data + made `eval_seed_auth_user_with_password` self-heal token columns on its idempotent path so it can't recur. |
| T073b | T073 fix-forward driven by F036 eval — six bugs unit tests missed | ✅ Code: `MultiStepComposer` gained `dialogLabel` prop (stops dialog↔step-input accessible-name collision) + Skip `role="link"` (DLS-aligned); `/you/sell` Add-a-product is now a `<button>`; `sellCreateLocationAction` uses pg pool (RLS bypass); `sellActivateAction` rewritten to call `place_for_coords` + walk parent_id chain (fixes a fictional FK join); AnchorLocationStep tracks `addedLocations` locally so the picker re-renders the inline-added Location with its label. 74/74 src vitest GREEN. M2 self-review PROCEED. F036 eval: **5/9 passing** (was 2/9). Four remaining are forward-deps: F035 Group public page (:112, :167), F038/F040/F034 item composers (:224), F035/F037 settings link (:266) — all logged DEVIATIONS accepted-as-is. SPEC-PATCHES queued for `places.geography` polygon seed (T058 substrate gap that always trips `place_for_coords` at b1). |
| T073a | T073 fix-forward — Sell-side `locations` column-name correction (`display_name` → `label`) + `sellCreateLocationAction` insert completed (member_id + slug + geography NOT NULL columns added) | ✅ Two-file fix + 1 regression unit test. Surfaced by the F036 fixture seed (the first writer to consult the live `locations` schema). 43/43 src/ vitest GREEN. Action-layer conformance OK. No migration. M2 self-review PROCEED — mechanical schema alignment. |
| T073 | `<SellWalkthrough>` + /you Sell CTA + getDraftGroup resume detector + `/you/sell` index stub + server-action wrappers for group.create/.update_draft/.activate (F036 surface) | ✅ Build complete on branch `t073` (commit `bf1ed11`, merged to main as `23c3710`). Files: `src/lib/sell/getDraftGroup.{ts,test.ts}`, `src/components/sell/{SellWalkthrough,SellCta}.{tsx,test.tsx}`, `src/app/you/sell/{actions.ts,page.tsx}`, /you page mounts `<SellCta>`. 73/73 src/ vitest GREEN (12 getDraftGroup + 20 SellWalkthrough + 9 SellCta + 32 prior). Action-layer conformance OK. M2 self-review PROCEED after 3 fix-now landings — brand-re-edit double-create guard (state.draftGroupId is never populated; shadowDraftId is the workaround); sellActivateAction refuses to guess a place-scoped URL (throws shop_url_unresolved if the join fails rather than 404 on F035); SellCta toast auto-clears after 4s. Locality (Tier 0) step is UI-only at b1 — `member_business_jurisdictions` substrate ships with F037; flag-for-spec-revision per SPEC-PATCHES. `sellCreateLocationAction` writes directly to `locations` table — `location.create` handler does not yet exist; flag-for-spec-revision. Anchor-Location picker may render empty at b1 (no Member↔Location anchor model post T061); AddEntityDrawer covers the gap. /you/sell index stub renders one "Add a product" link per active Shop with `href="#"` — F038/F040/F034 wire the real composers. Full focus-trap deferred to shared T071/T072 a11y follow-up. |
| T077 | F038 item action handlers — `item.create` / `item.publish` / `item.attach_location` + EventTable extension + registry | ✅ Build complete on branch `t77`. `src/actions/item/{create,publish,attach-location,index}.ts`; `_lib/event-log.ts` `EventTable` gains `item_events`/`location_events` + target-column cases; registered in `src/actions/index.ts`. `item.create` does the F038 one-transaction path (items + item_products + optional item_locations + item.created/.location_attached/.published), derives `brand_label` from `group_businesses.display_name`, owner-of-business-group authz; `item.publish` idempotent owner-only publish; `item.attach_location` owner-only attach. 24/24 T077 vitest + 152/152 src GREEN; tsc clean on new files; action-layer conformance OK (35 protected tables). M2 PROCEED (2 low-sev suggestions logged: cross-Member Location status='pending' forward-dep; benign concurrent double-publish). DEVIATIONS: `schedule_kind='permanent'` (F038 Data-Captured) → `'ongoing'` (schema CHECK has no 'permanent'). No migration (015/020 schema sufficient). |
| T078 | F038 product composer surface — `<ProductComposer>` + `createProductAction` + `<AddProductButton>` /you/sell wiring | ✅ Build complete on branch `t77`. `src/components/sell/ProductComposer.tsx` (MultiStepComposer; steps Details → Pickup → optional Where-is-this-made [F038 SKIP path] → Review/Publish; `dollarsToCents` helper; Free toggle → priceCents null); `src/app/you/sell/product/actions.ts` `createProductAction` (wraps `item.create` publish=true, assembles `/p/…/g/<slug>/p/<title>-<id8>` or `/m/<handle>/p/<title>-<id8>`); `src/components/sell/AddProductButton.tsx` client island replacing the T073 inert button (role=button + `/Add a product/i` preserved); `/you/sell/page.tsx` resolves anchor-Location labels for pickup pre-select. 8/8 ProductComposer vitest + 106/106 src GREEN; tsc clean on new files; conformance OK. M2 PROCEED. Photo-upload UI out of scope (F038); made-step is skip-only (F039 lands the Place picker). |
| T080 | Prep — generalize `item.create` for multi-kind (product/service/gathering) + data-driven `/you/sell` composer row | 🟡 Build complete on branch `t80` (worktree `../web-t80`), **not merged**. `item.create` kind widened `z.literal('product')` → `z.enum(['product','service','gathering'])`; child insert branches per kind (`item_products` / `item_services` rate_model default `'quote'` / `item_gatherings` host_member_id=member_id); `made_at_place_id` nulled for non-product (respects `items_made_at_only_on_products` CHECK); spine/owner-check/brand_label/location/publish stay kind-agnostic (product path byte-identical). `/you/sell/page.tsx` composer row extracted to a data-driven `COMPOSERS` array (F040/F034 each append one entry; placeholder `<p>` dropped). 9/9 T080 + 35/35 (T080+T077) vitest GREEN; F035/F036/F038 suites 144 GREEN; tsc + eslint clean on changed files. Full-suite 15 failures are pre-existing (migration snapshots + flaky CI-enforcement subprocess) — identical set on clean `main`. M2 Approve. DEVIATIONS: none. |
| T081 | F034 gathering composer — `<GatheringComposer>` + `createGatheringAction` + `<AddGatheringButton>` /you/sell wiring | 🟡 Build complete on branch `t-f034` (worktree `../web-tf034`), **not merged**. `src/components/sell/GatheringComposer.tsx` (MultiStepComposer; steps Kind [one-time / recurring / open-meetup — user language, not a four-kind picker] → Details → Schedule [future-only date/time; recurring derives weekly RRULE; open-meetup needs no time; optional capacity/cost/what-to-bring] → Review/Publish; `weeklyRrule`/`combineDateTime` helpers; blank cost → costCents null); `src/app/you/sell/gathering/actions.ts` `createGatheringAction` (wraps `item.create` kind='gathering' publish=true, maps kind→schedule_kind, assembles `/p/…/g/<slug>/e/<title>-<id8>` or `/m/<handle>/e/<title>-<id8>`); `src/components/sell/AddGatheringButton.tsx` appended to the data-driven COMPOSERS array. **`item.create` gathering arm extended** with `whatToBring` (input + insert). 8/8 GatheringComposer vitest + 146/146 src GREEN; tsc + eslint clean; conformance OK. M2 self-review PROCEED. Venue (`/l/<location>/e/`) entry deferred to F033 — b1 files under Group/Member (DEVIATIONS). |
| T082 | F034 public gathering Item page — `resolveGathering` + `<GatheringPublicPage>` + `/e/` catch-all dispatch + `/m/[handle]/e/[slug]` route | 🟡 Build complete on branch `t-f034` (worktree `../web-tf034`), **not merged**. `src/lib/items/resolve-gathering.ts` (`splitGatheringSlug` [inner `/e/` marker], `describeRecurrence`, `nextOccurrence`, `resolveGathering` — reuses `parseIdFragment`; RLS `items_select_published` is the visibility gate; id8-fragment addressing); `src/components/item/GatheringPublicPage.tsx` (title/next-occurrence/recurrence-in-human-terms/location-with-map-pin/cost-or-Free/capacity/what-to-bring/owner-link) + `src/components/item/ShareLinkButton.tsx` (native share / clipboard copy); gathering dispatch folded into `src/app/p/[...slug]/page.tsx` **before** the Group split; new `src/app/m/[handle]/e/[slug]/page.tsx` route. 17/17 (resolve 12 + page 5) vitest + 146/146 src GREEN; tsc clean; conformance OK. M2 self-review PROCEED. |
| T079 | F038 public product Item page — `resolveProduct` + `<ProductPublicPage>` + `/p` catch-all item dispatch + `/m/[handle]/p/[slug]` route | ✅ Build complete on branch `t77`. `src/lib/items/resolve-product.ts` (`splitItemSlug`, `parseIdFragment`, `resolveProduct` — RLS `items_select_published` is the visibility gate; id8-fragment addressing since `items` has no slug column — see DEVIATIONS); `src/components/item/ProductPublicPage.tsx` (title/price-or-Free/photo/pickup/brand-resolve-up-link/owner-link; Locally Made badge data-gated on `made_at_place_id` → empty on F038 skip-path); item dispatch folded into `src/app/p/[...slug]/page.tsx` **before** the Group split (else the shop page swallows the item URL); new `src/app/m/[handle]/p/[slug]/page.tsx` individual-seller route. 15/15 T079 vitest + 121/121 src GREEN; tsc clean on new files; conformance OK. M2 PROCEED (id8 collision 1-in-4B noted); M3 basic. **F038 surface complete build-side.** |
| T081 | F040 service arm of `item.create` — write `item_services.service_area_geography` (PostGIS circle from center+radius) | ✅ Build complete on branch `t-f040`. `src/actions/item/create.ts`: `itemCreateInput` gains `serviceAreaCenterLat/Lon/RadiusMeters` (optional, range-checked); service arm branches — when all three present, `item_services` insert computes `service_area_geography` via `st_buffer(st_setsrid(st_makepoint($lon,$lat),4326)::geography,$m)`, else inserts without geography. rate_model default `'quote'`, rate_cents passthrough. 7/7 T081 vitest; T077/T080 regression GREEN. M2 PROCEED. No migration (015 `item_services` sufficient). DEVIATIONS: rate_model honors shipped enum vs scenario list (SPEC-PATCHES filed); MultiPolygon edge unguarded (low-sev). |
| T082 | F040 service composer surface — `<ServiceComposer>` + `createServiceAction` + `<AddServiceButton>` + /you/sell entry | ✅ Build complete on branch `t-f040`. `src/components/sell/ServiceComposer.tsx` (MultiStepComposer; steps Details → Pricing [rate-model select + Free toggle + rate; quote/free → null] → Service area [center Location picker + radius miles] → Review/Publish; `milesToMeters` helper; **no Where-is-this-made step** — services excluded); `src/app/you/sell/service/actions.ts` `createServiceAction` (resolves center Location coords, wraps `item.create` kind='service' publish=true, assembles `/p/…/g/<slug>/s/<title>-<id8>` or `/m/<handle>/s/<title>-<id8>`); `src/components/sell/AddServiceButton.tsx` client island; `service` entry appended to the T080 `COMPOSERS` array (`/Add a service/i` accessible name). 10/10 ServiceComposer vitest GREEN; tsc + eslint clean. M2 PROCEED. DEVIATIONS: anchor Location doubles as service-area center (area-only deferred). |
| T083 | F040 public service Item page — `resolveService` + `<ServicePublicPage>` + `/p` catch-all `/s/` dispatch + `/m/[handle]/s/[slug]` route | ✅ Build complete on branch `t-f040`. `src/lib/items/resolve-service.ts` (`splitServiceSlug`, reuses `parseIdFragment`, `resolveService` — RLS `items_select_published` gate; reports `hasServiceArea`); `src/components/item/ServicePublicPage.tsx` (title/rate-by-model [hourly $X/hr · flat $X · membership $X/mo · quote "Request a quote" · null "Free"]/service-area section/brand-resolve-up-link/owner-link; **no Locally Made badge** — services excluded); service dispatch folded into `src/app/p/[...slug]/page.tsx` before the Group split (`/s/` marker); new `src/app/m/[handle]/s/[slug]/page.tsx` route. 16/16 (resolver + page) vitest GREEN; tsc clean; conformance OK. M2 PROCEED; M3 basic. DEVIATIONS: service-area renders as static text at b1 (Mapbox circle deferred — geography's feed-intersection job verified by eval). **F040 surface complete build-side.** |
| T075 | S-jurisdictions substrate — `member_business_jurisdictions` Tier 0 + `zip_metro_crosswalk` + `public.zip_is_proximal_to_location()` + `.set`/`.remove` handlers | 🟡 Build complete on branch `t75` (worktree `../web-t75`), **not merged**. Migrations `024_member_business_jurisdictions.sql` (table + active-row partial-unique index + public-read RLS + `member_events` event_kind CHECK extended with `business_jurisdiction_set`/`_removed`) + `025_zip_metro_crosswalk.sql` (crosswalk table + RLS public-read + inlined 90-row Sacramento CBSA-40900 seed + `places.msa_code` + `places` 40900 backfill + `locations.place_id` + SECURITY-DEFINER `zip_is_proximal_to_location()` w/ grants). Handlers `src/actions/member/business-jurisdiction-{set,remove}.ts` (owner-of-business-Group authz via `group_memberships.left_at`/`role='owner'`/`kind='business'`; `.set` soft-replaces prior active row → fresh `self_attested`; `.remove` soft-deletes; both same-transaction `appendEvent`); registered in `src/actions/index.ts` + member barrel. Seed artifact `supabase/seeds/zip_metro_crosswalk_sacramento.sql`; SQL contract test `supabase/tests/zip_is_proximal_to_location.sql`. 40/40 T075 vitest GREEN; migrations + SQL contract (5/5 cases) validated against live local Supabase (rolled back); tsc + eslint clean on changed files; action-layer conformance OK. Full-suite 15 failures pre-existing (migration snapshots + flaky CI subprocess; fewer than `main`'s 20). M2 PROCEED — 2 issues fixed before commit (crosswalk RLS for Rule 3; seed inlined to avoid `\ir` on `supabase db push`). DEVIATIONS: `places.msa_code` + `locations.place_id` added (spec assumed them; 2 SPEC-PATCHES filed); crosswalk RLS vs ticket; inline seed vs `\ir`; `left_at` vs `removed_at`. **Substrate gate for F037 + F039 closed.** |
| T076 | Sacramento-region places polygon + centroid seed (substrate) | 🟡 Build complete on branch `t76` (worktree `../web-t76`), **not merged**. Migration `026_places_polygon_centroid_seed.sql`: adds `places.centroid geography(Point,4326)` + GiST `idx_places_centroid` (NOT NULL + not-deleted predicate); inserts 3 cities (Davis→Yolo, Roseville→Placer, Folsom→Sacramento county); backfills polygons for the 11 T058-seeded rows (CA, 3 counties, Sacramento+West Sacramento cities, 5 neighbourhoods); derives `centroid` via ST_Centroid w/ ST_PointOnSurface fallback (+`metadata.centroid_method`); one `place_events` row per touched place (place.created×3 / place.updated×11) sharing one `correlation_id`, acting=system Member. **No metro/region row** (D3). New vitest: `tests/places-polygon-seed.test.ts` + `tests/places-reverse-geocode.test.ts` + `tests/places-poly-fixtures.ts` — 42 GREEN (pure-JS point-in-polygon/centroid-containment/smallest-covering + static SQL shape). `check:action-layer` clean. M2 Approve (3 nits accepted). DEVIATIONS: approx bbox polygons (full-res→S-metro), Placer pre-exists (backfill not insert), no centroid-tiebreak in 022, vitest=JS-geometry not live-DB. Pre-existing 16 suite failures (frozen migration snapshots + flaky CI-enforcement) unchanged on `main`. Live-DB containment = downstream Playwright `test` step. |
| T086 | F030 — `member.interests.add` handler + onboarding handler registration | 🟡 Build complete on branch `t-f030`, **not merged**. New `src/actions/member/interests-add.ts` (`member.interests.add`; idempotent `on conflict do nothing`; one `member.interest_added` event per newly-inserted tag; rejects `self-bootstrap`). Registered `member.interests.add` + `member.place_interest.add` + `member.place_interest.remove` (the last two built in T062 but never added to the registry) in `src/actions/index.ts` + barrel + re-exports. No migration (`member.interest_added` already in the event_kind CHECK). 11/11 T086 vitest GREEN; conformance OK. M2 self-review PROCEED. |
| T087 | F030 — locality feed query (`locality_feed_items` SQL fn + read helper) | 🟡 Build complete on branch `t-f030`, **not merged**. Migration `027_locality_feed.sql`: `public.locality_feed_items(p_place_id, p_tags, p_limit)` selects published `discoverable_items` whose `nearest_location_geography` `st_intersects` the chosen Place polygon (descendants live inside the ancestor polygon → no recursive walk), tag-match-first then `published_at desc`; `stable`/`security invoker`; granted anon+authenticated. `src/lib/feed/locality-feed.ts`: `getLocalityFeed` (rpc + row→FeedItem map), `widenLocality` (parent Place for the empty-state), `normalizeTags`/`clampLimit`. Place feed not Location feed (anti-Nextdoor). 9/9 T087 vitest GREEN. M2 self-review PROCEED. |
| T088 | F030 — anonymous home feed at `/` + signup CTA | 🟡 Build complete on branch `t-f030`, **not merged**. `src/components/feed/{LocalityFeed (async server),ItemFeedCard,FeedEmptyState,MakeThisYoursBanner,ScopePicker}.tsx` + `src/lib/feed/{feed-place,item-url}.ts`; `/` repointed from the pre-rebuild `HomeFeed` to `<LocalityFeed>`. Resolves the Place (Member primary_home → `?place=<slug>` → launch default), pulls the feed, renders banner (anon) + picker + cards, or widen-locality empty-state. b1 IP geolocation **deferred** → launch-locality default + picker; cards link via the Member-scoped Item URL (`/m/<handle>/<seg>/<slug>-<id8>`). 13/13 T088 vitest GREEN; src 189/189. M2 self-review PROCEED; M3 deferred (async server component). |
| T089 | F030 — newcomer onboarding (profile · locality · interests) | 🟡 Build complete on branch `t-f030`, **not merged**. `src/app/onboarding/{page,actions}.ts` + `src/components/onboarding/OnboardingFlow.tsx` (3 steps on `MultiStepComposer`; each step writes on Continue) + `src/lib/onboarding/{handles,interest-vocab}.ts`. Locality → `member.place_interest.add` (primary_home), interests → `member.interests.add` (both action-layer, events emitted); profile (display_name/handle/bio/pronouns/avatar) writes directly via owner-update RLS (no `member.update` handler — SPEC-PATCHES). Signup `next` defaults to `/onboarding`; `/onboarding` redirects unauth→signup and already-onboarded→`/`. **Auth-method gate resolved (b1): magic-link primary, social b2, email/password = eval path.** 9/9 T089 vitest GREEN. M2 self-review PROCEED; M3 deferred. **F030 eval authored** (`evals/features/F030-…spec.ts` + `evals/fixtures/F030-newcomer.ts`) — run pending (no live server in build sandbox). |
| T090 | F030 — email/password signup with returning-user detection | 🟡 Build complete on branch `t-f030`, **not merged**. **PM-directed auth-method change (supersedes T089's magic-link-primary): email/password PRIMARY, magic-link SECONDARY.** Migration `028_email_is_registered.sql` (`public.email_is_registered(email)` SECURITY DEFINER reads `auth.users`; anon-grant — enumeration tradeoff documented). `useAuth().checkEmailRegistered`. `src/components/auth/EmailFirstSignup.tsx` — single email step → unknown email "Set a password" (`signUp`) / registered "Enter password" (`signInWithPassword`); magic-link secondary; `signUp` live-session→`next` (default `/onboarding`), no-session→"confirm your email"; already-registered race → enter-password fallback. `/auth/signup` rewritten to use it (login page keeps `AuthMethods`). 9/9 T090 vitest; 198/198 src GREEN; tsc/eslint/conformance clean. F030 eval AC2/AC3 rewritten to drive the real signup (new + returning) via `signUpWithPassword`/`signInViaEmailFirst` helpers; stale `F003-registration-auth.spec.ts` rewritten to the email-first flow. M2 self-review PROCEED. |
| T091 | F032 — member.follow / member.unfollow handlers + server actions | 🟢 Merged to main. `src/actions/member/follow.ts` — `memberFollow` / `memberUnfollow`; self-follow + `self-bootstrap` sentinel guards pre-transaction; upsert-revive on re-follow (`on conflict … do update set unfollowed_at = null`); soft-unfollow stamps `unfollowed_at` (no delete); `member.followed` / `member.unfollowed` events (member_id = follower, payload.followed_member_id). Barrel + registry wired; server actions `src/app/m/[handle]/actions.ts`. No migration (substrate from T048). 12/12 `tests/actions-t091.test.ts` GREEN; row+event verified by F032 eval. |
| T092 | F032 — public Member page `/m/[handle]` (read + follow) | 🟢 Merged to main. `resolveMemberPage` (`src/lib/member/resolve-member-page.ts`) + `src/app/m/[handle]/page.tsx` + `<MemberPublicPage>` + `<FollowMemberButton>` client island (optimistic Follow⇄Following toggle; anon → `/auth/login?next=/m/<handle>`; self → Edit-profile). Migration `029_member_public_projections.sql` (additive: GRANT `member_has_standing_presence` to anon + new `member_public_group_memberships` projection view — listed groups only). Privacy: only `discoverability='listed'` groups surface; place-interests never queried; soft-deleted/nonexistent handle → 404. 6/6 resolver + 8/8 component vitest + **F032 Playwright eval 6/6 GREEN** (run-mode, live local Supabase). tsc clean on new files; eslint + action-layer conformance OK. M2 self-review PROCEED; M3 basic. DEVIATIONS + SPEC-PATCHES filed (member.md public read surface). |
| T093 | F041 — QR card lib + `item.qr_card.request` handler | 🟢 Merged to main. `src/lib/items/qr-card.ts` (kind-aware canonical-path builders `memberScopedItemPath`/`groupScopedItemPath`, `buildItemSlug`, `qrCardFilename`, `absoluteItemUrl`, `generateQrCardPng` → 1200px = 4in @ 300 DPI PNG via `qrcode`). `src/actions/item/qr-card.ts` (`item.qr_card.request`; owner-only AuthorizationError; non-published ConflictError; resolves canonical URL — group place-path CTE → member fallback; logs `item.qr_card_requested`). Barrel + registry wiring. No migration (`item.qr_card_requested` already in the `item_events` CHECK). 16/16 vitest (`qr-card.test.ts` 11 + `actions-t093.test.ts` 10, incl. absolute-URL) GREEN; tsc/eslint/conformance clean. M2 self-review PROCEED. |
| T094 | F041 — QR card surface ("Get a QR card" button) + eval | 🟢 Merged to main. `src/lib/items/is-item-owner.ts` (owner gate; members.id===auth uid) + test. `src/app/m/[handle]/qr-actions.ts` (`requestQrCardAction`; passes `NEXT_PUBLIC_SITE_URL` base). `src/components/item/QrCardButton.tsx` (`'use client'`, `.btn-secondary`, data-URI download, busy + error states). Owner-only wiring into product/service/gathering pages — both member-scoped (`/m/[handle]/{p,s,e}`) and place-scoped (`/p/[...slug]`) routes. **F041 eval 4/4 GREEN** (owner sees button on product + service; anon does not; click downloads a valid PNG). 17/17 F038+F040+F034 regression evals still GREEN. M2 self-review PROCEED; M3 self-review PROCEED (new `QrCardButton`). |
| T072 | `<AddEntityDrawer>` secondary-drawer sub-flow + dev demo page (F036 surface; reusable for any composer needing inline entity creation) | ✅ Single-form drawer at `src/components/composer/AddEntityDrawer.tsx` per `product/ui/design-language.md § Surface patterns → Add new entity inside a composer`. Stacks above the parent composer at `z-[60]` over `z-50`. Cancel / Add-and-select navigation row; no Continue, no Skip. Validates inline; surfaces submit error with `role=alert` + `aria-live=assertive`; ESC + X + Cancel all fire `onCancel`; tap-outside OFF; focus restored to opener on unmount. **Nesting refusal** via `AddEntityDrawerNestingContext` — a second drawer mounted inside the first throws "Cannot nest AddEntityDrawer" at render. 10/10 vitest GREEN. M2 self-review PROCEED with aria-live add. M3 satisfied at the basic level (full focus-trap + parent-pause -8px/60% styling deferred to a shared a11y follow-up with T071). Dev demo at `/add-entity-demo` (auto-gated by T071a's `(dev)/layout.tsx`). Commit `3276666`; merged to main as `08d7667`. |

### b1.x substrate sprint summary (2026-05-25 — closing)

6 migrations (017–022) + 5 action handlers + 1 reverse-geocoder TS module + 1 path resolver + 1 breadcrumb component + 1 Next.js route + 1 test helper + 14 test files.

- **Unit tests:** 107/107 green.
- **Phase 1 evals:** 181/181 green (includes 5 new spec files: places, member-place-interests, member-saved-searches, reverse-geocode, place-routing).
- **`npm run check:action-layer`:** clean. 35 protected tables (3 added — places + member_place_interests + member_saved_searches + place_events; 1 removed — member_location_affinities).
- **Lint + TypeScript:** clean on all sprint files.
- **M2 (engineering:code-review):** **Approve** after 2 medium-severity privacy-leak fixes auto-landed (saved-search update/remove existence-vs-auth distinction). 5 suggestions logged in DEVIATIONS § 2026-05-25.
- **M4 (deploy-checklist):** drafted at [`operations/deploy-checklist-b1x.md`](../operations/deploy-checklist-b1x.md). PM sign-off items remain (apply migrations to prod Supabase BEFORE the web deploy; ratify the `member_location_affinities` drop is safe against prod data).
- **Pre-existing `migrations-t04*.test.ts` failures** are independent of this sprint (brittle `toEqual` pattern broken since T055).

**Sprint exit criterion met.** All 7 work items implemented and verified against the live local Supabase.

## Rebuild on Primitives — Phase 0 (AI-native floor)

The b1 marketplace shipped T001–T026; T027 deferred. The 2026-05-10 PM decision to rebuild on Person / Item / Location / Group primitives supersedes the prior 7-phase migration plan with a 4-phase clean-slate rebuild. T028–T040 were drafted against the prior plan and are STALE-banned. T041–T044 are the Phase 0 ticket set per [`/notes/migration-to-primitives.md`](../notes/migration-to-primitives.md).

**Phase 0 — AI-native floor (4 tickets):**

- **T041 — Postgres extensions + embedding tables.** Build complete 2026-05-10. Wiped six legacy migrations (001–006), wrote three Phase 0 migrations (001_extensions, 004_item_embeddings, 005_member_embeddings). 15 file-shape assertions passing. Runtime verified locally: extensions + tables present in Supabase. Eval-run pending T043+ test helpers.
- **T042 — Members + member_events floor + system Member.** Build complete 2026-05-10. Single consolidated migration `002_members.sql` (originally split into 002 + 002a + 002b; consolidated when Supabase CLI was found to silently skip alpha-suffixed filenames — see DEVIATIONS). Three logical sections: members table + RLS + indexes + updated_at trigger; member_events table monthly-partitioned + audit fields + rotation functions; system Member row + self-bootstrap event. `web/src/lib/system-member.ts` mirrors the SQL constants. 38 file-shape assertions passing. **Going-forward rule:** all migration filenames must match `^\d+_[a-z0-9_]+\.sql$` — enforced by the test suite.
- **T043 — Action layer scaffold + `member.create` handler.** Build complete 2026-05-10. New `web/src/actions/` tree: `_lib/{errors,context,handler,db,audit,event-log,handle-derivation}.ts` + `member/{index,create}.ts` + `index.ts` registry. `web/src/lib/action-context.ts` resolver. `web/scripts/check-action-layer-conformance.ts` greps for direct writes; wired as `npm run check:action-layer`. New deps: `pg`, `@types/pg`, `zod`, `tsx`. Transaction wrapper uses `pg.Pool` directly + `BEGIN/COMMIT/ROLLBACK` (Supabase JS lacks transactions). Tests: 59/60 sandbox-side; full Vitest run + DB-runtime assertions land via Playwright eval.
- **T044 — Supabase Auth signup hook.** Build complete 2026-05-10. Migration `006_auth_signup_hook.sql` enables `pg_net` + `pgcrypto`, defines `handle_new_auth_user()` (security-definer, HMAC-SHA256 signs payload, fires `net.http_post` async to a Next.js route), attaches AFTER INSERT trigger on `auth.users`. Route at `web/src/app/api/internal/auth-signup/route.ts` (Node runtime) validates signature via constant-time HMAC compare, invokes `member.create` with `actingMemberId='self-bootstrap'`, maps ActionError → HTTP status. Sentinel-proxy fix to `web/src/lib/action-context.ts` (T043 was leaking pool clients). 33 sandbox file-shape assertions + ~28 Vitest route-mock assertions. Phase 0 exit criterion verifiable end-to-end via Playwright eval (helpers pending).

## Phase 0 Status: ADR-15 fix-forward landed 2026-05-18

The 2026-05-17 PM eval run (`npx playwright test evals/phase-0/floor.spec.ts`, 22 tests) surfaced 6 failures, all tracing to T047's `members_assert_id_in_auth_users` constraint trigger (committed 2026-05-11, post-dates the Phase 0 spec and the T052 bulk seed). Fix-forward landed `web/supabase/test-helpers/04_auth_user_seeding.sql` (new file: `handle_new_auth_user()` override with `eval.skip_signup_hook` GUC bypass + `eval_seed_auth_user_only(uuid, text)` SECURITY DEFINER helper) and updated 6 spec tests + the `eval_seed_handle_collision_range` helper to seed `auth.users` rows before any direct `public.members` insert. DEVIATIONS entry 2026-05-18 carries the full architectural narrative. Spec-side rerun pending PM (the agent sandbox can't reach the user's local Supabase + Next.js dev server).

## Phase 0 Status: BUILD COMPLETE 2026-05-10

All four Phase 0 tickets (T041–T044) have shipped build-side with passing file-shape and unit tests. Runtime verification of the full Phase 0 exit criterion (`spawn fresh auth.users → members row + member.created event with correct audit fields`) is the Playwright eval at `web/evals/phase-0/floor.spec.ts`, which runs end-to-end once the eval helper RPCs (`eval_member_create_with_failure_injection`, `eval_seed_handle_collision_range`, etc.) are provisioned — flagged as a separate stage.

Phase 0 substrate now installed:
- pgvector + postgis extensions
- Members table (b1 T1 column set) + RLS + indexes + trigger
- member_events partitioned event log + audit fields + rotation functions
- System Member row + self-bootstrap event
- Action layer (`web/src/actions/`) with `defineHandler` factory, transaction wrapper, audit injector, event-log writer, error taxonomy, registry
- `member.create` proof-of-pattern handler with collision-suffix logic
- Conformance-check script (`npm run check:action-layer`)
- Auth signup hook (Postgres trigger → Next.js route → `member.create`)

Ready for Phase 1 re-ticketing (T045+) per the rebuild plan.

After Phase 0 closes, Phase 1 re-ticketing (T045+) opens against the rebuild plan's full schema floor.

## Rebuild on Primitives — Phase 1 (Schema floor)

Phase 1 lands the primitive tables (locations, members augmentation, items, groups, discovery view) with RLS, indexes, action handlers, and event-log writers. Empty DB at the end; nothing user-visible yet.

**Phase 1 — Schema floor (in progress):**

- **T045 — Locations spine + 3 children + events.** Build complete 2026-05-11. Single migration `007_locations.sql` carrying: spine (`public.locations`, all b1 + reserved columns, four indexes incl. GIST on geography, RLS public-read + owner-update, no INSERT/DELETE policies); three child tables (`location_permanent`, `location_recurring_temporary`, `location_areas`) with per-child public-read mirrors on the spine's discoverability via EXISTS subquery; `sync_area_centroid()` security-definer trigger function that writes `ST_Centroid(polygon)` to the spine's geography on insert/update of `location_areas`; `public.location_events` partitioned monthly with the b1 emitted + reserved event_kind enum, audit fields (`acting_member_id` NOT NULL, `via_delegation_id` reserved), composite PK `(id, created_at)`, two indexes, owner-read RLS, and `ensure_location_events_partition` / `rotate_location_events_partitions` rotation functions seeded with current + 2 future months. 46 file-shape assertions passing (sandbox plain-node). Numbering note: rebuild plan called this `008_*`; renumbered to `007_*` per the Phase 1 dependency reorder — see DEVIATIONS.md. Commit `fab7fd9`.
- **T046 — Locations RLS fix-forward.** Build complete 2026-05-11. Migration `008_locations_owner_read.sql` lands the three corrective items the T045 M2 code review surfaced: (a) new `locations_owner_read` RLS policy closing the matrix per `location.md` line 165 (owners can now SELECT their own private + soft-deleted-protected rows); (b) `idx_locations_geog` swapped from full to partial (`where deleted_at is null`) matching `location.md` line 136 so soft-deleted Locations don't bloat the proximity index; (c) `sync_area_centroid()` rewritten with `set search_path = public, extensions` for defensive robustness against Supabase's PostGIS-relocation pattern in newer distributions. 6 file-shape assertions passing (sandbox plain-node). The verification-ladder discussion from the same review is captured in `product/exploration/locally-owned-verification.md` and queued for `pipeline-product`.
- **T048 — Member interests + follows.** Build complete 2026-05-11. Single migration `010_member_interests_follows.sql`: (a) `public.member_interests` with composite PK `(member_id, tag)`, tag CHECK (lowercase alnum + hyphen, 1–60 chars), `idx_member_interests_tag` for inverse-lookup ("which Members declare `live-music`?"), public-read RLS only — interests power Item relevance + Group suggestion + locality-first index scoring; (b) `public.member_follows` with composite PK `(follower_member_id, followed_member_id)`, table-level CHECK preventing self-follow, soft-unfollow via nullable `unfollowed_at`, two partial indexes (`idx_follows_followed_active` + `idx_follows_follower_active` — both `where unfollowed_at is null` for the two follower-direction surfaces), single `member_follows_public_read` policy `using (true)` per the **2026-05-11 product re-scope on follow visibility** (M2 surfaced a critical RLS bug in the originally-specced dual-EXISTS-against-member_privacy pattern; PM used the review window to re-scope — follow graph is community-fabric, the load-bearing privacy work shifts to `member_location_affinities` in T049 where `lives`/`works` rows go through SECURITY DEFINER functions per `member.md` lines 295-298). `member_privacy.show_following` / `show_followers` columns from T047 remain as reserved substrate. No INSERT/UPDATE/DELETE policies on either table — action-layer-only writes. 19 file-shape assertions passing (sandbox plain-node mirroring `web/tests/migrations-t048.test.ts`). DEVIATIONS entry + `member.md` flag both reference the re-scope.
- **T051 — Action layer CI enforcement (four rules).** Build complete 2026-05-12. Four CI defenses against the McKinsey/Lily failure mode: (a) **Rule 1** lives in `eslint.config.mjs` as `no-restricted-imports` (forbids `pg` runtime import + bare `createClient` from `@supabase/supabase-js` outside `src/actions/_lib/**`; type-only imports allowed via `allowTypeImports`) plus `no-restricted-syntax` (forbids `process.env.SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_SECRET_KEY` outside the same scope); (b) **Rule 2** lives in `scripts/check-action-layer-conformance.ts` as `checkRouteHandlerImports` — any `src/app/api/**/route.ts` exporting `POST`/`PUT`/`PATCH`/`DELETE` must `import ... from '@/actions/...'` or carry a `// action-layer:exempt` annotation matched by an entry in `scripts/action-layer-exemptions.json` with `path` / `reason` (>=10 chars) / `expires_at` (ISO-8601, not in the past) / `follow_up_ticket` (`/^T\d{3}$/`); ledger ships empty `[]` because all flagged routes were zombies and deleted; (c) **Rule 3** lives in `tests/rls-coverage.test.ts` — Vitest queries `pg_tables` and fails if any public table has `rowsecurity = false` (allowlist empty; skips gracefully when `DATABASE_URL` is unset for sandbox-mode dev); (d) **Rule 4** lives in the conformance script as `checkParameterizedQueries` — flags `${...}` in `.query` / `.rpc` template literals unless an inline `// sql-injection-safe: enum-constrained by <TypeName>` annotation matches `/^[A-Z][A-Za-z0-9_]+$/`; annotation may appear on the call line or within 3 lines above (multi-line SQL literals can't carry JS comments inside the SQL body). `src/actions/_lib/event-log.ts` was annotated with `// sql-injection-safe: enum-constrained by EventTable` above its `ctx.db.query` call. Test inventory: `tests/ci-enforcement-rule-1.test.ts` (4 cases), `tests/ci-enforcement-rule-2.test.ts` (5 cases incl. malformed-ledger), `tests/ci-enforcement-rule-4.test.ts` (5 cases), `tests/rls-coverage.test.ts` (1 case, DB-skipif). Probe directories `src/__lint_probe__/`, `src/__sql_probe__/`, `src/app/api/__probe__/` added to `.gitignore`. **Zombie sweep:** deleted 9 pre-rebuild API routes (`/api/admin/*`, `/api/vendor/*`, `/api/bulletins/*`, `/api/cron/*`, `/api/jobs/*`), `src/lib/admin.ts`, and the orphaned admin/ page surface (`src/app/admin/**`). Per rebuild plan § "What we delete from current web/." Documentation: `web/CLAUDE.md` § "Writing a new API route" lands the four-rule checklist; `product/systems/action-layer.md` § "Policy posture" gains the CI-enforcement subsection. **Sandbox note:** Vitest 4 + rolldown segfaults under Linux x86_64 in this build environment; tests were verified by exercising the conformance script and ESLint directly with probe files (every probe scenario reproduced manually). Vitest run on darwin is the user's verification step.

- **T049 — Member location affinities.** Build complete 2026-05-17. Single migration `011_member_location_affinities.sql` lands the multi-Location belonging substrate per `member.md` lines 261-306 and ADR-16. Table carries composite PK `(member_id, location_id, affinity_kind)`, six-value `affinity_kind` CHECK (`lives`/`works`/`plays`/`visits`/`follows`/`liked`), `removed_at` soft-remove. Three partial indexes: `idx_affinity_member_active` on `(member_id, affinity_kind) where removed_at is null` (Member's own surfaces), `idx_affinity_location_followers` on `(location_id) where affinity_kind = 'follows' and removed_at is null` (Concerts-in-the-Park feed), `idx_affinity_location_locals` on `(location_id, affinity_kind) where affinity_kind in ('lives','works') and removed_at is null` (locally-owned derivation in `groups.md`). ADR-16 privacy posture: single owner-read RLS policy keyed by `auth.uid()` — no public/peer/per-kind read; no INSERT/UPDATE/DELETE policy (action-layer-only writes per ADR-7 + T051 CI enforcement). Three SECURITY DEFINER scalar functions implement the public access catalog — `member_is_local_to_location(uuid, uuid) returns boolean` (groups.md's locality badge), `count_likes_for_location(uuid) returns integer` and `count_followers_for_location(uuid) returns integer` (Location-page rollups). All three `language sql`, `STABLE`, `set search_path = public`, granted `execute` to `authenticated, anon`. Tests: 22-case Vitest suite at `tests/migrations-t049.test.ts` + 49-check plain-node sandbox runner at `scripts/t049-sandbox-check.mjs` (Vitest segfaults under Linux x86_64 in the build sandbox; the script is the sandbox verifier). All 49 passing; `npm run check:action-layer` clean. DEVIATIONS entries (2026-05-17): sandbox runner committed alongside Vitest; functions `language sql` not `plpgsql` (planner inlining); spec patches to `member.md` / `groups.md` / `policy-framework.md` flagged for `pipeline-product`. Runtime verification of the five Studio probes pending the user's `supabase db reset` pass.

- **T050 — Agent-assistance substrate.** Build complete 2026-05-17. Single migration `012_member_agent_assistance.sql` lands three sections in one file per the rebuild plan's `007g_` + `007h_` consolidation (alpha-suffix prohibition + the FK retrofits gate both tables together): (1) `public.member_self_records` — Member-owned context document; `member_id uuid primary key references public.members(id) on delete cascade`; `document jsonb default '{}'::jsonb`; `scratch_or_full text default 'scratch'` with CHECK; `updated_at` trigger reusing `public.update_updated_at_column()`; RLS owner-read + owner-update (the owner-update policy carries explicit `with check` per the T047 polish); no INSERT/DELETE policy; **no bootstrap trigger on public.members** — deliberate exception to the row-per-Member-at-signup pattern because the surface ships b2+ and most Members will never opt into agent assistance (DEVIATIONS entry, action handler `member.self_record.update` does insert-or-update on first write). (2) `public.member_delegations` — scoped expiring permission grants; `id uuid primary key default gen_random_uuid()`; `member_id` FK on delete cascade; `grantee_label` 1..120 char CHECK; `scopes text[]` with `array_length(scopes, 1) >= 1` CHECK (controlled vocabulary enforced by the action handler `member.delegation.grant`, not the schema — same pattern as `member_interests.tag`); `granted_at` / `expires_at` / `revoked_at` / `metadata jsonb`; partial index `idx_delegations_member_active on (member_id) where revoked_at is null` (deliberately simplified from `member.md` line 393's `where revoked_at is null and (expires_at is null or expires_at > now())` because partial-index predicates are evaluated at INSERT time, not query time — DEVIATIONS entry flags member.md for `pipeline-product`); RLS owner-read only (delegations carry the full surface of agent capabilities — peer-read would be reconnaissance for prompt-injection / capability-misuse); no INSERT/UPDATE/DELETE policy. (3) **FK retrofits — close the via_delegation_id circle.** `alter table public.member_events add constraint member_events_via_delegation_fkey foreign key (via_delegation_id) references public.member_delegations(id) on delete set null not valid;` + `validate constraint`; same retrofit on `public.location_events`. Both via the two-step `not valid` + `validate` pattern; `on delete set null` because hard-deleting a Delegation must not cascade-delete the append-only event rows per ADR-7's append-only invariant (the audit field becomes null, preserving the row's other truth). Tests: 4-block Vitest suite at `tests/migrations-t050.test.ts` (directory state, `member_self_records` shape + RLS + no-bootstrap negative check, `member_delegations` shape + RLS + simplified-index assertion, FK retrofits) + 35-check sandbox mirror at `scripts/t050-sandbox-check.mjs`. All 35 passing; `npm run check:action-layer` clean. **M2 `engineering:code-review` PROCEED** 2026-05-17 — no required fixes; three nit findings logged (ADR-10 → ADR-7 retag sweep across 002/007/012, retired `member.maker_mode_changed` event_kind, partitioned-FK safety confirmed). Runtime verification of the five Studio probes pending the user's `supabase db reset` pass.

- **T052 — Phase 0 eval helpers (test-helpers folder + bootstrap script).** Build complete 2026-05-17. Unblocks `pipeline-eval` run-mode on T041–T044 (and T051's Rule 3 RLS coverage, which shares the local-DB bootstrap step). Per ADR-18: new folder `web/supabase/test-helpers/` carries four `.sql` files (`00_introspection.sql` — `eval_pg_extensions` + `eval_table_shape` reading from `pg_attribute` + `format_type` so vector columns report as `vector(N)` rather than information_schema's `USER-DEFINED` + `eval_is_partitioned`; `01_conformance.sql` — `eval_artifacts` KV table + `eval_conformance_check_result()` that raises a clear P0002 if bootstrap was not run; `02_action_failure_injection.sql` — `eval_member_create_with_failure_injection(p_id)` implementing Path A per ADR-18 Decision 2, subtransaction + `when not_null_violation` + `when foreign_key_violation` defense-in-depth secondary catch; `03_handle_collisions.sql` — `eval_seed_handle_collision_range(p_base, p_count)` with upfront `char_length` precondition guards because `on conflict do nothing` does NOT catch `check_violation`, plus `eval_clear_handle_collision_range(p_base)` using `LIKE` not regex concatenation to keep T051 Rule 4 quiet). Every function is `SECURITY DEFINER` with `revoke execute … from public; grant execute … to service_role`. New bootstrap script `web/scripts/bootstrap-eval-helpers.ts` enforces the production-safety constraint: hard-exits (code 2) unless `DATABASE_URL` host is in `{localhost, 127.0.0.1, ::1, host.docker.internal}` or `SUPABASE_ENV=local` is set without a URL (then falls back to standard supabase CLI port). Applies the four SQL files lexicographically via `pg.Client`, then `execFile`s `tsx check-action-layer-conformance.ts --json` and upserts the parsed result into `public.eval_artifacts` under `key='conformance_check'`. `check-action-layer-conformance.ts` extended with `--json` mode (single JSON object on stdout, exit-code semantics unchanged) and an `ALLOWED_EXCEPTIONS` entry for `scripts/bootstrap-eval-helpers.ts` (the one place where the action-layer-conformance rule has a hole; the hole is explicit, named, and confined to a single path). `web/supabase/test-helpers/**` is implicitly exempt because the file lister filters to `.ts/.tsx`. `npm run eval:bootstrap` wired in `package.json`. Tests: Vitest suite at `web/tests/eval-bootstrap.test.ts` (layout, guard negatives, `--json` contract) + `web/tests/ci-conformance-json.test.ts` (`--json` mode against the conformance script directly with positive + negative probes) + plain-node sandbox runner at `web/scripts/t052-sandbox-check.mjs` mirroring all assertions (Vitest segfaults under Linux x86_64 per BUILD-LOG T051 note; the script is the sandbox verifier). **59 sandbox checks passing**; `npm run check:action-layer` clean; ESLint clean for files touched (incidental cleanup: dropped unused `statSync` import in `check-action-layer-conformance.ts`). **M2 `engineering:code-review` PROCEED** 2026-05-17 with two fix-now items applied in the same loop (secondary `foreign_key_violation` catch in Helper 5; `char_length` precondition guards in Helper 6). **No new files in `web/supabase/migrations/`** — production schema unchanged. Runtime verification (Phase 0 Playwright spec end-to-end against `supabase start`) handed to `pipeline-eval` run-mode and to the user's manual smoke: `supabase start && npm run eval:bootstrap && npx playwright test evals/phase-0/floor.spec.ts`.

- **T047 — Members augmentation: FK + privacy + handle history.** Build complete 2026-05-11. Single migration `009_members_phase1.sql` carrying: (a) `members.home_location_id` FK to `public.locations(id) on delete set null` via the two-step `not valid` + `validate constraint` pattern; (b) `assert_member_id_in_auth_users()` constraint trigger (security definer, `search_path = public, auth`, DEFERRABLE INITIALLY DEFERRED) substituting for the impossible `members.id → auth.users(id)` FK — system-Member id is exempted explicitly inside the function body; (c) `public.member_privacy` per `member.md` Data-model-implications (eight columns with CHECK + default per ADR-9 opt-out posture) + `member_privacy_set_updated_at` trigger + owner-read + owner-update RLS (no INSERT/DELETE — action-layer-only; UPDATE policy carries explicit `with check` per M2 polish); (d) `create_member_privacy_defaults()` bootstrap trigger fires AFTER INSERT on `public.members`, inserting a defaults row keyed by `new.id` with `on conflict do nothing` (function carries `comment on function` per M2 polish); (e) explicit `member_privacy` backfill row for the system Member (bootstrap trigger fires only on future inserts); (f) `public.member_handle_history` T2-placeholder table (composite PK `(member_id, handle)`, handle CHECK regex + length, owner-read RLS only, no other indexes). NO `primary_group_id` FK — `public.groups` doesn't exist yet; deferred to a Phase 1 Members FK closeout ticket after T0NN-groups. **M2 code review PROCEED 2026-05-11** — three non-blocking suggestions applied as fast-follow (explicit `with check`; `comment on function`; trigger-ordering documentation in the migration header). 27 file-shape assertions passing (sandbox plain-node mirroring the Vitest suite at `web/tests/migrations-t047.test.ts`). Numbering: rebuild plan called this `007_*`; locations took 007/008 in the Phase 1 dependency reorder, so this lands as 009 — recorded in DEVIATIONS.md.

## Remaining b1 MVP Work

Three tickets close the marketplace loop. Recommended order:

1. **T025** — Vendor bulletin compose + delivery. Unblocks the pinned bulletin section in T024 (currently has nothing to render).
2. **T026** — Vendor founder dashboard. Backs the vendor recruitment pitch.
3. **T019** — Geocoding + pin confirmation. Trust hardening on registration; can run in parallel with T025/T026.

See [planning/now/bundle-1.md](../planning/now/bundle-1.md) for the full MVP definition and success metrics.

## Latest Commits

- T046: Locations RLS fix-forward (`008_locations_owner_read.sql`)
- T045: Locations spine + 3 children + events (Phase 1 — `007_locations.sql`) — `fab7fd9`
- T026: Vendor founder dashboard — `/you/vendor` Overview/Followers/Activity tabs, MetricCards + Sparklines, ListingHealth, Top Tasks aside, CSV export, migration 006 `rollup_vendor_stats_daily`
- T025: Vendor bulletin compose + delivery — `/you/vendor/bulletins/*`, fan-out API, mute, open/unsubscribe primitives, migration 005
- T024: Events-driven Home feed — `EventCard`, filter chips, pinned bulletin section, lazy `market_session` generation, click telemetry
- T023: `/you` restructure — Saved / Following / Settings tabs, Your Market row, vendor mode entry
- T021: Tide accent + CTA patterns — `AuthGateModal`, two-track nav, sticky mobile CTA, recruitment panel on Home
- T022: Foundational schema — events, vendor_bulletins, bulletin_deliveries, vendor_events, vendor_stats_daily; follows soft-delete
- T012–T018: Etsy-style feed pivot — schema, bottom nav, home feed, explore, market modal, vendor profile, follow + notifications
- T011: Report a concern form
- T010: Support button (heart toggle)
- T009: Shareable listing page with OG metadata
- T008: Business registration form
- T007: Business detail card with ownership badge
- T006: Map search by category and location
- T005: Pin clustering at low zoom levels
- T004: Map view with colored ownership pins
- T003: Authentication (sign-up, login, sign-out, middleware)
- T002: Database schema and types
- T001: Project initialization

## Blockers

(None)

## Notes

Next.js 16 + Tailwind v4 + Supabase + Mapbox GL JS. T024 added migration `004_system_runs.sql` and a new `/api/jobs/generate-market-sessions` endpoint — run `supabase db push` before first deploy. Latest test count: 51/51 passing post-T024.
